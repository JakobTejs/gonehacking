<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jakob.roedekors">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DonerActivity"
            android:label="@string/title_activity_doner" />
        <activity
            android:name=".MinSideActivity"
            android:label="@string/title_activity_min_side" />
        <activity
            android:name=".FrivilligActivity"
            android:label="@string/title_activity_frivillig" />
        <activity
            android:name=".DonationbetalingskortActivity"
            android:label="@string/title_activity_donationbetalingskort" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".QRkodeActivity"
            android:label="@string/title_activity_qrkode" />
        <activity
            android:name=".IndsamlerforsideActivity"
            android:label="@string/title_activity_indsamlerforside" />
        <activity
            android:name=".OenskRuteActivity"
            android:label="@string/title_activity_oensk_rute" />
        <activity
            android:name=".MaanedeligBetalingActivity"
            android:label="@string/title_activity_maanedelig_betaling" />
        <activity
            android:name=".MinRuteActivity"
            android:label="@string/title_activity_min_rute" />
        <activity
            android:name=".nyhedActivity"
            android:label="@string/title_activity_nyhed" />
        <activity
            android:name=".butikkerActivity"
            android:label="@string/title_activity_butikker" />
        <activity
            android:name=".MinGruppeActivity"
            android:label="@string/title_activity_min_gruppe" />
        <activity
            android:name=".gruppenActivity"
            android:label="@string/title_activity_gruppen" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".RuteActivity"
            android:label="@string/title_activity_rute"></activity>
    </application>

</manifest>
